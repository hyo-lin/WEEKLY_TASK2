apiVersion: v1
kind: Service
metadata:
  name: mondcommunity-backend-svc
  labels:
    app: mondcommunity
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: mondcommunity
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP